Gem::Specification.new do |s|
  s.name = %q{json_translations}
  s.version = JsonTranslations::VERSION
  s.summary = %q{Allows for model based translations}
  s.files = `git ls-files`.split("\n")
  s.require_paths = ["lib"]
end
